spring:

  datasource:
#    url: jdbc:mysql://localhost:${DB_PORT}/${DB_NAME}
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
    url: jdbc:mysql://localhost:3306/architecture
    username: root
    password: Alimnikas299
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update

  logging:
    level:
      sql: info
      web: info
  data:
    redis:
      port: 6379
      host: localhost
      database: 0

#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: quest
#    password: quest
server:
  port: 8082
